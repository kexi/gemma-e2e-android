{
  // "bun" types: AdbClient spawns adb through Bun.spawn, and the tests run on
  // bun:test.
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "types": ["bun"]
  },
  "include": ["src/**/*.ts"]
}
